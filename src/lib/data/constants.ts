/** Shared pagination limits for read models */
export const PAGE_SIZE = 15;
export const RANGE_RECORD_LIMIT = 100;
/** Employee dashboard history preview — KPIs still use the full selected range. */
export const DASHBOARD_HISTORY_PREVIEW_LIMIT = 7;
